# Proxima Daily Briefing — dipendenze Python
# Versioni pinned per riproducibilità

# Universe building globale
tradingview-screener>=2.5.0,<3.0
finvizfinance>=1.0,<2.0          # fallback US

# Fondamentali e dati
yfinance>=0.2.40,<0.3
pandas>=2.0,<3.0
numpy>=1.24,<3.0

# LLM
anthropic>=0.69,<1.0             # >=0.69 per adaptive thinking + output_config (Opus 4.8)

# Render HTML
jinja2>=3.1,<4.0
markdown>=3.5,<4.0

# Delivery (opzionali, ma comodi)
requests>=2.31,<3.0              # Telegram/webhook
